#include<stdio.h>
int main()
{
 int *a,temp,i,j,n;
 printf("enter the size of the array\n" );
  scanf("%d"&n );
  a = (int*)malloc(sizeof(int)*n);
  for ( i = 0; i < n; i++) {
    /* code */
    scanf("%d",&a[i] );
  }
 printf("after sorting\n" );
 for ( i = 1; i < n; i++) {
   temp = a[i];
   while(temp < a[])

 }

 return 0;
}
