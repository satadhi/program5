#include<stdio.h>

sort(a,q)
{ if( q > 2)
  int q = partition(a,n);
}

int main()
{
  int a = {14,23,55,62,13,2,52,9,89};
  sort(a,9);
  for (size_t i = 0; i < 9; i++) {
    printf("%d ",a[i] );
  }

}
